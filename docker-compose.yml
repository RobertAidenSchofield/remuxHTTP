services:
  remux:
    build:
      context: .
      dockerfile: docker/Dockerfile.multistage
    image: remux:local
    container_name: remux
    restart: unless-stopped
    ports:
      - '3000:3000'
    volumes:
      - ./data:/data
    environment:
      - PORT=3000
      - DATA_DIR=/data

